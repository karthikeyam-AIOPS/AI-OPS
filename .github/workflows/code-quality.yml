name: Code Quality (DISABLED)

# DISABLED - This workflow is temporarily disabled  
# Use basic-ci.yml for now

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - name: Placeholder
      run: echo "Code quality checks are disabled"