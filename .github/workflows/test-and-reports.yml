name: Tests and Reports (DISABLED)

# DISABLED - This workflow is temporarily disabled due to complexity issues
# Use basic-ci.yml for now

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - name: Placeholder
      run: echo "This workflow is disabled"