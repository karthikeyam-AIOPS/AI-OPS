name: Basic CI (DISABLED)

# DISABLED - Temporarily disabled due to Black formatting issues
# Will be re-enabled once formatting issues are resolved

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
    - name: Placeholder
      run: echo "Basic CI is temporarily disabled"